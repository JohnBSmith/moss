
function deep_copy(x)
   T = type(x)
   if T is List
      return x.map(deep_copy)
   elif T is Map
      return map([key,deep_copy(value)] for key,value in x.items())
   else
      return x
   end
end
