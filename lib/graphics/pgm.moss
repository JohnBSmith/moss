
begin
public load_img

use graphics: load_font
use fs: open

class Pgm = {
   function draw(canvas,x,y)
      canvas.glyph(x,y,self.data,0,self.width,self.height)
   end
}

function load_img(path)
   data = open(path).read()
   return table Pgm(record(load_font(data)))
end



end
