
# wrong line in error message

use math: sqrt

function mean(a)
  a.sum()/size(a)
end

function sigma(a,m=null)
  if m is null
    m = mean(a)
  end
  sqrt(a.sum(|x| (x-m)^2)/(size(a)-1))
end

Stat = table{
  function string
    """\
    mean  = {},\n\
    sigma = {}\
    """ % [self.mean, self.sigma]
  end1
}

function stat(a)
  m = mean(a)
  table Stat{mean = m, sigma = sigma(a,m)}
end

a = [1,2,3,4]
print(stat(a))


